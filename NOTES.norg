* Neovim
** Images In Neovim
   I couldn't figure out how to get the `magick` luarock installed because homebrew installed luarock 5.4
   and `magick` only works on 5.1. So the `magick` (pun intended) command is:
@code bash
luarocks --local --lua-version=5.1 install magick
   @end
* Tmux
* Zsh
* Wezterm
* Yabai
* SKHD
* Scripts
